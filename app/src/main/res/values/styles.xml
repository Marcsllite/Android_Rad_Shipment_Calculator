<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/umlBlue</item>
        <item name="colorPrimaryDark">@color/darkColor</item>
        <item name="colorAccent">@color/defaultGrey</item>
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.MinWidth">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/defaultGrey</item>
        <item name="colorPrimaryDark">@color/darkColor</item>
        <item name="colorAccent">@color/umlBlue</item>
    </style>

</resources>
